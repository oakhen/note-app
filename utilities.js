function add(a, b) {
  return a + b;
}

const remove = (argv) => {
  return argv;
};
module.exports = {
  remove,
  add,
};


/* so this sucker is in zen mode and the keybinding for zen mode is not easy */


